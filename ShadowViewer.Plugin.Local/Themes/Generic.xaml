<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MangaReader.Controls"
    xmlns:local2="using:ShadowViewer.Plugin.Local.Controls"
    xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml">

    <Style BasedOn="{StaticResource DefaultSliderStyle}" TargetType="local2:ReaderSlider" />
    <Style TargetType="local2:LocalReader">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:LocalReader">
                    <Grid HorizontalAlignment="Stretch">
                        <xaml:CanvasVirtualControl x:Name="PART_Canvas" />
                        <ProgressRing
                            Width="32"
                            Height="32"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsIndeterminate="True"
                            Visibility="{TemplateBinding IsLoading}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultMangaReaderControlStyle" TargetType="controls:MangaReader">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:MangaReader">
                    <Grid>
                        <xaml:CanvasAnimatedControl x:Name="PART_MainCanvas" />
                        <Border
                            x:Name="PART_InfoPanel"
                            Margin="20"
                            Padding="12"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            IsHitTestVisible="True"
                            Visibility="Collapsed">
                            <StackPanel Width="160" Spacing="8">
                                <TextBlock
                                    Opacity="0.7"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="Zoom Level" />
                                <Grid ColumnDefinitions="*, Auto" ColumnSpacing="8">
                                    <Slider
                                        x:Name="PART_ZoomSlider"
                                        VerticalAlignment="Center"
                                        Maximum="500"
                                        Minimum="10"
                                        Value="100" />
                                    <TextBlock
                                        x:Name="PART_ZoomText"
                                        Grid.Column="1"
                                        Width="35"
                                        VerticalAlignment="Center"
                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                        Text="100%"
                                        TextAlignment="Right" />
                                </Grid>

                                <TextBlock
                                    Margin="0,4,0,0"
                                    Opacity="0.7"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="Offset" />
                                <TextBlock
                                    x:Name="PART_PositionText"
                                    FontFamily="Consolas"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="X: 0, Y: 0" />

                                <Button
                                    x:Name="PART_ResetButton"
                                    Margin="0,4,0,0"
                                    HorizontalAlignment="Stretch"
                                    Content="Reset View" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>